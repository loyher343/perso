angular.module('comicApp').controller('loginCtrl', function($scope,loginSrv) {
    
    $scope.test = 'Tis better to be vile than vile esteemed'

    // $scope.user = user.data && user.data.err ? user.data.err : user;
})